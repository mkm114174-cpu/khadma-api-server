# Required
DATABASE_URL=postgresql://user:pass@host/neondb?sslmode=require
NEON_AUTH_BASE_URL=https://ep-green-brook-aso0ob6f.neonauth.c-4.eu-central-1.aws.neon.tech/neondb/auth
PORT=8080
NODE_ENV=production

# Object storage — on Render set GCS_SERVICE_ACCOUNT_JSON + bucket paths (Replit Object Storage paths work if migrated).
# GCS_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
# PUBLIC_OBJECT_SEARCH_PATHS=/your-bucket/public
# PRIVATE_OBJECT_DIR=/your-bucket/private
