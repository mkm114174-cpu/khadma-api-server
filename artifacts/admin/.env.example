# Neon Auth URL — same value as NEON_AUTH_BASE_URL on the API server.
# From Neon Console → Project → Auth → Configuration
VITE_NEON_AUTH_URL=https://ep-xxx.neonauth.region.aws.neon.build/neondb/auth
